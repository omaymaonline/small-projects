# 🔐 Guess Password

A small Python guessing game where the computer generates a random four-digit PIN and the user tries to guess it.

### Concepts practised

`import` · `random` · `input()` · `random.randint()` · `if / else` · f-strings
