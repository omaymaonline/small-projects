# 💸 Who Pays?

A small Python project that takes a list of friends and randomly chooses one person to pay.

### Concepts practised

`import` · `random` · `lists` · `input()` · `random.choice()` · `split()`
