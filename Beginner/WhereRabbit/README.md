# 🐰 Where Should the Rabbit Go?

A small Python project where the user chooses a row and column, and the program places a rabbit 🐰 in that position in a garden.

### Concepts practised

`lists` · `nested lists` · `input()` · `indexing` · `int()` · `remove()` · `insert()` · `if/elif/else`
