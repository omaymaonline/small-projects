# 🔐 Secret Message Encoder/Decoder

A small Python project that encodes a message by reversing its words and the resulting string, then decodes it back to the original message.

### Concepts practised

`input()` · `split()` · `join()` · `slicing` · `list comprehensions` · `strings`
